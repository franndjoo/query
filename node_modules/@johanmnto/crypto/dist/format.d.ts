export declare namespace Format {
    /** remove all special characters */
    function escape(toFormat: string): string;
    /** restore a string which has got through `Format.escape` */
    function unescape(toUnescape: string): string;
}
