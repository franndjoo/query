/** list of flags that defines how a query string behave */
export enum QueryFlags {
    ForEachItem = "FEI<",
}

export enum QueryFlagsToken {
    FlagsDefinitionOpenTag = "[fl",
    FlagsDefinitionCloseTag = "fl]"
}